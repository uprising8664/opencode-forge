{
  "$schema": "https://opencode.ai/config.json",

  // ─── Global Defaults ───────────────────────────────────────────────────────
  // Agents are auto-discovered from .opencode/agents/*.md — no duplication here.
  "default_agent": "smith"
}
